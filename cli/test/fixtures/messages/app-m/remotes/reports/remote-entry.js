import { t } from '@core/localization/i18n.js';

// Its own bundle answers this one, and the shell's answers the second.
export const heading = () => t('reports.title');
export const shellHeading = () => t('orders.title');
